package string;

import org.junit.Test;
import static org.junit.Assert.assertEquals;

public class keepTest {
    @Test
    public void test_() {
        assertEquals(new keep().keepStringPackage(), "String Package");
    }
}
